import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./index.css"; // Можно добавить общий стиль для body

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
